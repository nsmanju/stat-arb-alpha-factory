import sys
sys.path.insert(0, 'src')  # fix for bad imports

import cointegration as co
import ou_process as ou
import backtest as bt
import factors as fa

breakpoint()

print("Paused - type your pdb commands now")
